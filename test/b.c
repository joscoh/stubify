#include "b.h"

int orthogA(Aa *a, Aa *b) {
  int x;
  //lots of spaces to ensure a preprocessor line number is in here























  RETURN (composeA(a, b) == 0);
}

int normA(Aa *a) {
  return composeA(a, a);
}
